---
tags: [phase-1, 显存, 选卡, oom, lora, 实战, batch]
created: 2026-07-20
updated: 2026-07-29
status: reviewed
---
# 1.6 选模型 + 选显卡实战：从显存反推配置，避免 OOM

## 一句话
给定你的显卡（显存大小与卡数），用 [[1.5-显存分配精算]] 的公式**反推**能训的模型、并行方式与开关组合，在跑之前就确保不 OOM。

## 核心公式/原理

### 决策流程（自上而下）
```
显卡显存 M 与卡数 N
   │
   ▼
① 单卡能否装下 actor 训练态 16P/N ?
   ├─ 否 → 降模型 / 加卡 / 开 offload / 用 LoRA
   ▼
② rollout 推理态(2P + KV) 能否与训练态共存 ?
   ├─ 否 → 调低 gpu_memory_utilization / offload actor 参数
   ▼
③ 激活是否过大 ?
   ├─ 是 → gradient checkpointing + remove_padding + 减 micro_batch
   ▼
④ 留 10–20% 余量 → 得最终配置
```

### 手段一：LoRA 大幅降低训练态
全参训练态是 $16P$；LoRA 只训练注入的低秩矩阵（参数量常为全参的 0.1%–1%），训练态显存近似：
$$\text{LoRA 训练态} \approx \underbrace{2P}_{\text{冻结base权重}} + \underbrace{16 \cdot P_{lora}}_{\text{可训参数}}$$
$P_{lora} \ll P$，所以训练态从 $16P$ 降到约 $2P$ + 一点点。

> [!success] LoRA 是小资源的救命稻草
> Qwen3-1.7B 全参训练态 ≈27GB（单卡 4090 装不下）；换 LoRA 后训练态 ≈ $2×1.7$ + 少量 ≈ 4–6GB，单卡 4090 轻松装下，还能给 rollout 留足显存。
> 注意：verl 中 actor 与 reference colocate 的设计，正好支持"ref 就是 LoRA 的 base model"，实现高效 LoRA PPO/GRPO。
> 来源：verl HybridFlow Guide（actor/ref colocate 用于高效 LoRA）。

### 手段二：offload 把训练态卸到 CPU
| 开关 | 作用 | 代价 |
|---|---|---|
| `actor.fsdp_config.param_offload=True` | actor 参数卸 CPU | 取用时 PCIe 传输变慢 |
| `actor.fsdp_config.optimizer_offload=True` | 优化器状态卸 CPU | 更新变慢 |
| `ref.fsdp_config.param_offload=True` | ref 参数卸 CPU | 算 ref_log_prob 变慢 |
| `*.enable_activation_offload=True` | 激活卸 CPU（仅 FSDP） | 配合 gradient ckpt |

> [!warning] offload 省显存但慢
> offload 用时间换空间。小资源（显存不足）时不得不用；大集群（显存充足）时通常 `param_offload=False, optimizer_offload=False` 追求速度。ref 模型几乎总是 offload（用得少）。

### 手段三：缩小 micro-batch + 动态 batch
- `*_micro_batch_size_per_gpu` 直接决定激活大小，是 OOM 时**最先调小**的旋钮。
- `use_dynamic_bsz=True` 按 token 数动态组 batch，长序列不爆显存，吞吐更高。
- 详见 [[3.5-batch体系]]、[[4.5-OOM排查决策树]]。

### batch 三级关系速查（选卡必懂）

> [!question] 为什么选卡要懂 batch 三级
> 选卡算显存时，"每卡处理多少序列"由 micro_batch 决定（直接影响激活显存）。但 micro_batch 又受 train_batch_size 和 ppo_mini_batch_size 约束（整除关系）。不懂三级关系，算出的显存预算会错——要么整除报错跑不起来，要么低估激活峰值导致 OOM。

**三级 batch 各管什么**
| 层级 | 参数 | 作用域 | 管什么 |
|---|---|---|---|
| L1 全局 | `data.train_batch_size` | 全局 | 一步处理多少个 **prompt**（算法量） |
| L2 更新 | `actor.ppo_mini_batch_size` | 全局 | 一次 actor 更新用多少条 **response**（算法量） |
| L3 前向 | `*_micro_batch_size_per_gpu` | **每卡** | 每卡每次前向/反向的序列数（**性能量，直接决定激活显存**） |

**总 response 数**：$B_{total} = \text{train\_batch\_size} \times \text{rollout}.n$（每个 prompt 采 $n$ 条）。

**三级的推导链**（从全局到每卡）
```
train_batch_size（全局 prompt 数）
   × rollout.n（每 prompt 采 n 条）
   = B_total（全局 response 总数）
   ÷ ppo_mini_batch_size（每次更新用多少）
   = 一步内 actor 更新次数（mini-batch 轮数）
   ÷ world_size（GPU 数）
   = 每卡一次更新分到的序列数
   ÷ micro_batch_size_per_gpu（每卡每次前向）
   = 每卡前向/反向次数
```

**整除约束（跑不起来 vs 跑得起来的分界线）**
```
① train_batch_size × rollout.n  必须被  world_size 整除
② ppo_mini_batch_size  必须能整除  B_total
③ ppo_mini_batch_size / world_size = 每卡 mini batch  必须被  micro_batch_size_per_gpu 整除
```
> [!danger] 最常见报错
> `ppo_mini_batch_size should be larger than 0 after normalization`
> → 根因：`train_batch_size × rollout.n` 不能被 world_size 整除。
> → 解法：调 `rollout.n` 或 `train_batch_size`，使乘积可被 GPU 数整除。

**一个完整算例**（8 卡，Qwen3-8B GRPO 官方配置）
```
train_batch_size = 1024（prompt）
rollout.n = 5
world_size = 8
ppo_mini_batch_size = 256
ppo_micro_batch_size_per_gpu = 32

B_total = 1024 × 5 = 5120（全局 response）
actor 更新次数 = 5120 ÷ 256 = 20 轮
每卡每轮 = 256 ÷ 8 = 32 条
每卡每次前向 = 32（= micro_batch_size_per_gpu）→ 恰好 1 次前向搞定
整除检查：5120 % 8 = 0 ✅，256 % ... = 0 ✅，32 % 32 = 0 ✅
```

> [!important] 选卡时的关键认知
> - **算法量**（train_batch_size、ppo_mini_batch_size）影响"数据效率与稳定性"，**不直接决定单卡显存**——别拿它们当显存旋钮。
> - **性能量**（`*_micro_batch_size_per_gpu`）才是单卡激活显存的直接旋钮——OOM 时先调它。
> - 选卡算显存时，先定 train_batch_size/rollout.n/mini_batch（按算法需求），再算每卡 mini batch，最后反推能用的 micro_batch，用 micro_batch 估激活显存。

> [!tip] 三级速记
> - **train_batch_size**：全局 prompt 数（×n 得 response 总数）。
> - **ppo_mini_batch_size**：一次更新用多少 response（÷得更新轮数）。
> - **micro_batch_size_per_gpu**：每卡每次前向多少序列（**决定激活显存，OOM 第一旋钮**）。
> - 三者有严格整除链，配错直接报错跑不起来。

### 场景决策表（核心交付）

**场景 A：1× RTX 4090 (24GB)**
| 模型 | 方案 | actor 训练态 | rollout | 结论 |
|---|---|---|---|---|
| Qwen3-0.6B | 全参 + offload | 16×0.6≈10GB | g_util 0.5 | ✅ 可行 |
| Qwen3-1.7B | **LoRA** | ≈5GB | g_util 0.5 | ✅ 推荐 |
| Qwen3-1.7B | 全参 | ≈27GB | — | ❌ 装不下 |
| Qwen3-4B | LoRA + offload | ≈10GB | g_util 0.4 | ⚠️ 紧张可行 |

**场景 B：4× A6000 (48GB)**
| 模型 | 方案 | 配置要点 | 结论 |
|---|---|---|---|
| Qwen3-4B | 全参 FSDP/4 | 训练态 16×4/4≈16GB/卡 | ✅ 舒适 |
| Qwen3-8B | 全参 FSDP/4 + offload | 训练态≈33GB/卡 + ckpt | ⚠️ 紧张 |
| Qwen3-8B | LoRA FSDP/4 | 训练态≈4GB/卡 | ✅ 推荐 |

**场景 C：8× A100/H100 (80GB)**
| 模型 | 方案 | 配置要点 | 结论 |
|---|---|---|---|
| Qwen3-8B | 全参 FSDP/8 | 见 [[1.5-显存分配精算]] 预算表 | ✅ 官方示例档 |
| Qwen3-14B | 全参 FSDP/8 + ckpt | 训练态≈29GB/卡 | ⚠️ 需精算 |
| Qwen3-32B | 全参 FSDP/8 + 强 offload | 训练态≈64GB/卡 | ⚠️ 极限，建议多机 |

> [!tip] 经验法则
> - **全参训练**：单卡显存 ≥ $16P/N$ + rollout($2P$+KV) + 激活 + 余量。
> - **显存不够先想 LoRA**：能把训练态从 $16P$ 压到 $2P$ 量级。
> - **rollout 是弹性项**：OOM 先降 `gpu_memory_utilization`，再降 `micro_batch`。
> - **8B 全参的甜区是 8×80GB**；更小模型可在更少/更小卡上跑，更大模型要多机或 LoRA。

### 反推练习模板
```
我的显卡：____（型号）×____（卡数），单卡显存 ____ GB
目标模型：____，参数量 P = ____ B
方案：全参 / LoRA（可训参数 ____ B）
① actor 训练态 = 16P/N = ____ GB/卡（LoRA 则 2P + 16·P_lora）
② rollout = gpu_memory_utilization × 单卡显存 = ____ GB
③ 激活（ckpt? remove_padding? micro_batch=__）≈ ____ GB
④ ref/reward offload？ = ____
合计 ≈ ____ GB  vs  单卡 ____ GB → 余量 ____%（应 ≥10%）
```

## 与其他概念的关系
- **前置依赖**：[[1.3-模型参数量计算]]、[[1.4-量化基础]]、[[1.5-显存分配精算]]。
- **对比区别**：LoRA（省训练态）vs offload（省训练态但慢）vs 加卡（FSDP 分片）。
- **后续延伸**：[[4.2-小资源版脚本]]、[[4.3-集群版脚本]]（把本页决策落成具体命令）、[[5.5-分布式显存重分配]]（多卡精算）。

## 关键来源
- [verl Performance Tuning Guide](https://verl.readthedocs.io/en/latest/perf/perf_tuning.html), 2025 — offload、ckpt、micro_batch、dynamic bsz 调显存
- [verl HybridFlow Programming Guide](https://verl.readthedocs.io/en/latest/hybrid_flow.html), 2025 — actor/ref colocate 支持高效 LoRA
- 场景决策表中的显存数值 — 基于 [[1.5-显存分配精算]] 公式推算, Claude, 2026-07-20 ⚠️（建议实测校准）

## 我的实验
- （E1 收尾）用"反推练习模板"为你的硬件 + 目标模型填一份完整配置，作为 Phase 4 脚本的输入。把结果存到 `experiments/E1-显存预算表.md`。
